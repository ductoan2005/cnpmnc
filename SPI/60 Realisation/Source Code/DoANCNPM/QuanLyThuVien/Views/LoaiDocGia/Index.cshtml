@model IEnumerable<QuanLyThuVien.Models.LoaiDocGia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenDocGia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDocGia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LoaiDocGiaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LoaiDocGiaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LoaiDocGiaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
